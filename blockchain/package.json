{
    "name": "divel-blockchain",
    "version": "1.0.0",
    "description": "Sepolia blockchain deployment for Digital Evidence Locker",
    "scripts": {
        "test": "hardhat test",
        "compile": "hardhat compile",
        "node": "hardhat node",
        "deploy": "hardhat run scripts/deploy.js --network sepolia",
        "deploy:local": "hardhat run scripts/deploy.js --network localhost"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "hardhat": "^2.19.0",
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "dotenv": "^16.4.0"
    }
}